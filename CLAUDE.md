# elca portfolio

## 指示全般に関すること

- testやlintはcommit時に自動的に行われるので、 `npx` などで実施しないこと。
- 特に指示がない場合はcommitすること。
- 特に指示がない場合はpushしないこと。

## アプリケーションの特性

このアプリケーションには以下の概念が存在する。

- App: アプリケーション。ブログを閲覧できるBlogや自己紹介文があるAbout Meなど。
  - 基本的にWindow内で描画される。
  - `/blog` など特定のURLにリダイレクトすることで全画面表示することができる。
- Window: アプリケーションの表示領域。
  - `/blog` など `/[アプリケーション名]` にリダイレクトすると全画面に描画するようにする
- コントロールボタン: Windowの操作を行うボタン群。どのWindowにも、どの状態のWindowにも表示される。
  - closeボタン: Appを削除するボタン。削除されたAppは画面上からもDock上からも削除される(Appsからは残る)。全画面時は無効。
  - 縮小化ボタン: Windowを縮小するボタン。縮小すると、コントロールボタンとAppのタイトルのみ表示される。全画面時は無効。
  - 全画面ボタン: Windowを全画面表示(=`/[アプリケーション名]`にリダイレクト)するボタン。縮小化中は無効。
- Dock: 現在 `/` で表示しているAppの一覧と、Appsのボタン、コンパクト化ができる。
  - Appの一覧は、Dock内のアイコンをドラッグ&ドロップすることで順番を入れ替えることができる。その影響は `/` で表示されているAppの順番に影響する。
  - コンパクト化により、「コンパクト化ボタン」「Appsのボタン」の2つのみになる
- Apps: 全てのApp一覧。該当するAppをクリックするとDockに追加され、表示される。
